# 🤖 AiAntiblokBot

**AiAntiblokBot** — отказоустойчивый Telegram-бот для автоматизированного взаимодействия с пользователями,  
ориентированный на лид-магниты, контент-доступ по подписке и дальнейшее масштабирование в AI-ассистента.

Проект спроектирован с упором на **стабильную работу 24/7**, защиту от сбоев, минимальное потребление ресурсов  
и возможность безопасного развития функциональности без остановки сервиса.

---

## 🚀 Ключевые возможности

- ✅ Telegram-бот с polling-моделью
- ✅ Команда `/status` для мониторинга состояния
- ✅ Heartbeat-механизм (контроль «живости» процесса)
- ✅ Автоматический watchdog (перезапуск при сбоях)
- ✅ Защита от двойного запуска (`flock + pid`)
- ✅ Логирование событий и ошибок
- ✅ Поддержка режимов **FREE / PAID**
- ✅ Готовность к AI-модулям и knowledge-base

---

## 🧠 Архитектура и устойчивость

Проект построен по принципу **self-healing сервиса**:

```

cron (каждую минуту)
↓
watchdog.sh
↓
bot.py (Telegram polling)
↓
heartbeat.txt / bot.pid / bot.lock

```

Если бот:
- завершился с ошибкой,
- был убит,
- завис,
- потерял соединение,

👉 watchdog автоматически перезапустит его **без участия человека**.

---

## 📊 Команда /status

Команда доступна в Telegram и возвращает текущее состояние бота:

```

🤖 AiAntiblokBot
🆔 PID: 67548
⏱️ Uptime: 22s
❤️ Heartbeat age: 22s
📚 KB files: 0
⚙️ Mode: FREE
🐍 Python: 3.6.9

```

Назначение:
- диагностика
- контроль деплоя
- быстрый health-check

---

## 🗂️ Структура проекта

```

AiAntiblokBot/
├── bot.py                 # основной код бота
├── requirements.txt       # зависимости Python
├── README.md              # документация
│
├── data/
│   ├── bot.pid             # PID текущего процесса
│   ├── bot.lock            # защита от двойного запуска
│   ├── heartbeat.txt       # heartbeat-метка
│   └── sessions/           # данные сессий
│
├── logs/
│   ├── bot.log             # лог бота
│   └── watchdog.log        # лог watchdog
│
├── venv/                   # виртуальное окружение Python
└── cron_run.log             # лог cron-запусков

````

---

## ⚙️ Установка и запуск

### 1. Клонирование

```bash
git clone <repo>
cd AiAntiblokBot
````

### 2. Виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Переменные окружения

Создать файл `.env`:

```env
BOT_TOKEN=your_telegram_bot_token
```

---

## 🔁 Watchdog + cron

Watchdog запускается через cron **каждую минуту**.

Пример crontab:

```bash
* * * * * /home/.../AiAntiblokBot/data/watchdog.sh >> /home/.../AiAntiblokBot/logs/cron_run.log 2>&1
```

Это обеспечивает:

* автоматический старт
* автоперезапуск
* защиту от залипаний

---

## 🧩 Технологии

* Python 3.6+
* python-telegram-bot
* Bash (watchdog)
* Linux (cron, flock)
* Virtualenv

---

## 🔐 Безопасность и стабильность

* 🔒 Защита от двойного запуска
* 🧯 Автовосстановление после падений
* 📝 Полное логирование
* ⚠️ Контроль ошибок Telegram API
* 🧪 Проверка синтаксиса перед перезапуском

---

## 📈 Потенциал развития

Проект готов к расширению:

* AI-ассистент (LLM)
* Knowledge Base (документы, FAQ, PDF)
* Платный доступ
* Личный кабинет пользователя
* Интеграция с CRM / аналитикой
* Web-панель мониторинга

---

## 👤 Автор и владелец

**Алексей Бородулин**
Предприниматель, финансист, бизнес-тренер

🌐 [https://borodulin.expert](https://borodulin.expert)
📲 Telegram: @Borodulin_expert

---

## ⚠️ Дисклеймер

Проект находится в активной разработке.
Архитектура и подход ориентированы на продакшн-эксплуатацию.



## Диалог и сохранение контекста (Case Flow)

Бот ведёт пользователя по кейсу блокировки в формате диалога и НЕ теряет нить разговора.

### Как это работает
- При первом вопросе по теме (блокировка/115-ФЗ/комплаенс/ЗСК) бот создаёт `case` в `context.user_data`.
- Пока `case.active == True`, бот считает, что пользователь отвечает по кейсу:
  - даже если ответ короткий (“да”, “нет”, “ок”)
  - даже если ответ в формате “1. ... 2. ...”
  - даже если пользователь вставил сторонний вопрос (“а кто ты?”, “а сколько времени?”)

### Off-topic внутри кейса
Пользователи могут “поюзать” бота. Поэтому:
- если кейс активен и пользователь задаёт вопрос не по теме — бот коротко отвечает (или вежливо отказывает),
- затем СРАЗУ возвращается к кейсу: “Вернёмся к вашей блокировке…” и задаёт следующий вопрос.
Автопродолжение включено всегда.

### Сброс кейса
- `/reset` — полностью сбрасывает текущий кейс (case).
- `/start` — показывает меню.


# AiAntiblokBot — MVP (Python 3.6) + GigaChat + KB

## Быстрый старт

1) Заполните `.env` рядом с `bot.py`:
```env
BOT_TOKEN=...
REQUIRED_CHANNEL=@Borodulin_expert

GIGACHAT_AUTH_KEY=Basic <Authorization key>
GIGACHAT_SCOPE=GIGACHAT_API_PERS
```

2) Сборка материалов:
```bash
./venv/bin/python kb/rebuild_content.py
./venv/bin/python kb/rebuild_text_index.py
```

3) Запуск:
```bash
./venv/bin/python bot.py
```

## Папки

- `kb/handouts/` — раздатка (файлы)
- `kb/templates/` — шаблоны (файлы)
- `kb/text/` — база знаний (md)
- `kb/content.json` — генерится скриптом
- `kb/text_index.json` — индекс для поиска по базе знаний
